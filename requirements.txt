# 文学指纹分析系统 - 依赖清单（版本已锁定）
# 安装：pip install -r requirements.txt
#
# 说明：gunicorn 已移除——它是 Unix 专属部署服务器，
# 本项目在 Windows 下通过 start.bat / streamlit 直接运行，无需安装。

streamlit==1.52.2
flask==3.1.2
flask-cors==6.0.2
nltk==3.9.2
numpy==2.2.6
pandas==2.2.3
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
scipy==1.16.3
